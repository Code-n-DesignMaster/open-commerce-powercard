export interface IMarsCachingConfig {
  powercardBalanceExpirationSeconds: number;
  powercardValidExpirationSeconds: number;
  rateCardExpirationSeconds: number;
  locationsExpirationSeconds: number;
}
